
chore: add .gitignore

chore: add split requirements (base / dev / prod)

chore: scaffold full project directory skeleton

chore: add pre-commit hooks (black, isort, flake8, mypy)

chore: add pre-commit hooks and Makefile dev task runner

chore: complete .env.example schema — all service URLs, secrets, feat… 

feat(core): pydantic BaseSettings config loader, environment-aware (d… 

feat(app): app factory, lifespan hooks, exception hierarchy, router s… 

feat(db): async SQLAlchemy 2 engine, session factory, Alembic wired t… 

feat(cache): async Redis pool (redis-py asyncio), key builder registry

feat(ops): full health and readiness endpoints with per-dependency la… 

feat(logging): structlog pipeline — JSON/console renderer, UTC timest… 

feat(logging): request-ID middleware, level control utility, async-sa… 

feat(db): user, profile, token, interest, follow, post, reply, like m… 

feat(db): chat, chat_participants, message, message_edit, reaction, r… 

feat(db): notifications table - polymorphic entity ref, JSONB payload… 

docs(db): complete index performance plan - query mapping, raw DDL, i… 

feat(db): intial Alembic migration - all tables, indexes, enums, chec… 

docs(cache): complete Redis key naming convention — 14 namespace grou… 

feat(cache): token bucket Lua script, idempotency Lua script, Python … 

feat(cache): per-user rate limit wrapper, idempotency two-phase proto… 

feat(security): IP extraction with trusted proxy chain, input sanitiz… 

feat(workers): Celery app factory, 5 queues + DLQs, task routing, bea… 
